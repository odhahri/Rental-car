{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Python Debugger: SpeedRent app",
        "type": "debugpy",
        "request": "launch",
        "program": "${workspaceFolder}/manage.py",
        "args": ["runserver", "--noreload"],
        "django": true,
        "autoReload": {
          "enable": true
        }
      }
    ]
  }