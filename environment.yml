name: rental-car
channels:
  - conda-forge              # Add conda-forge as a channel
  - defaults                 # Keep defaults as a fallback
dependencies:
  - python=3.11
  - pip=24.2
  - make
  - pip:
      - -r requirements.txt
